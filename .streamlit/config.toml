[server]
# static/ 以下のファイルを /app/static/ で配信できるようにする（apple-touch-icon用）
enableStaticServing = true

[theme]
base = "light"
primaryColor = "#2F6FED"
backgroundColor = "#F5F7FA"
secondaryBackgroundColor = "#FBFCFE"
textColor = "#1A2230"
font = "sans serif"
